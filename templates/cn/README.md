# CN templates (alias index)

The 20 CN-style docx templates are at `templates/*.docx` (root layout, kept for backwards-compatibility with existing skill references). This directory exists so cross-jurisdiction code can refer to `templates/cn/` symmetrically with `templates/sg/` and `templates/us/`.
